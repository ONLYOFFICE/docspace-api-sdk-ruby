language: ruby
cache: bundler
rvm:
  - 2.7
  - 3.0
  - 3.1
script:
  - bundle install --path vendor/bundle
  - bundle exec rspec
  - gem build docspace-api-sdk.gemspec
  - gem install ./docspace-api-sdk-3.7.0.gem
